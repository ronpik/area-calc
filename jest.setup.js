// Jest setup file
// Reset modules before each test to ensure clean state
beforeEach(() => {
  jest.resetModules();
});

// Clear all mocks after each test
afterEach(() => {
  jest.clearAllMocks();
});
